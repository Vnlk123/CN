                     EXPERIMENT-10
Write a Program to implement Dijkstra‘s algorithm to compute the Shortest path through a graph. 
Dijkstra’s algorithm
Dijkstra’s algorithm is used to find the shortest path from a single source vertex to all other vertices in a 
weighted graph, where all edge weights are non-negative.
It ensures that for every vertex, the algorithm finds the minimum possible distance from the source.
Program
#include <stdio.h>
#define INF 999
void dijkstra(int n, int v, int cost[10][10], int dist[])
{
 int i, u, count, w, flag[10], min;
 for (i = 1; i <= n; i++)
 {
flag[i] = 0;
dist[i] = cost[v][i];
 }
 flag[v] = 1;
 dist[v] = 0;
 count = 1;
 while (count < n)
 {
min = INF;
for (w = 1; w <= n; w++)
 if (dist[w] < min && !flag[w])
 {
min = dist[w];
u = w;
 }
flag[u] = 1;
count++;
for (w = 1; w <= n; w++)
 if ((dist[u] + cost[u][w] < dist[w]) && !flag[w])
dist[w] = dist[u] + cost[u][w];
 }}
